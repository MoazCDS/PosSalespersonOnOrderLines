<odoo>
    <record id="pos_order_view_form_inherit_add_salesperson" model="ir.ui.view">
        <field name="name">pos.order.view.form.inherit.add.salesperson</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="salesperson_id" readonly="state != 'draft'"/>
            </xpath>

            <xpath expr="//field[@name='lines']/tree/field[@name='full_product_name']" position="after">
                <field name="salesperson_id"/>
            </xpath>
        </field>
    </record>
</odoo>